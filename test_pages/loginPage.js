/*let headers = $$('div[class="header-links"]>ul>li');
return headers;

const loginPage = function ()
{

    console.log("I am in the login page");

    async function clickButton (headerOption) {
        let preferredHeaderOption = this.headers;
        for (var i = 0; i < preferredHeaderOption.length; i++) {
            if (preferredHeaderOption[i].getText() === headerOption) {
                preferredHeaderOption[i].click();
                await browser.getTitle().eql('nopCommerce demo store. Login');
                break;
            }
        }
    }

}
module.exports = LoginPage;*/
